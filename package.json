{
  "name": "dump",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Dump — a calm, self-sorting research vault. Full-stack: Node + libSQL/Turso, deployable to Vercel.",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "engines": {
    "node": ">=18"
  }
}
